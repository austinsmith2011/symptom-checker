services:
  - type: web
    name: symptoms
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: OPENAI_API_KEY
        sync: false
    customDomains:
      - name: symptoms.appsthataustinmade.com
